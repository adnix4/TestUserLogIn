@page
@model TestUserLogIn.Pages.Members.SelectMemberInvolvementModel
@{
	ViewData["Title"] = "Select Member Involvement";
}

<h2 class="mb-4">Select the Ministries that you are interested in.</h2>

<div class="card shadow-sm mb-4" id="introBlock">
    <div class="card-body">
        <h5>
            “Then the righteous will answer him, ‘Lord, when did we see you hungry and feed you, or thirsty and give you a drink? When did we see you a stranger and welcome you, or lacking clothes and clothe you? When did we see you sick or in prison and visit you?’

            “The King will answer them, ‘Amen I tell you: Just as you did it for one of the least of these brothers of mine, you did it for me.’
        </h5>
        <div class="citedVerse">&minus; Matthew 25:37-40</div>
    </div>
</div>

<form method="post">
    <div class="card shadow-sm mb-4">
        <div class="card-body">
            <div class="row">
                @foreach (var involvement in Model.AllInvolvements)
                {
                    <div class="col-md-6 mb-2">
                        <div class="form-check">
                            <input class="form-check-input"
                                   type="checkbox"
                                   name="SelectedInvolvementIds"
                                   value="@involvement.InvolvementAreaID"
                                   id="interest_@involvement.InvolvementAreaID"
                                   @(Model.SelectedInvolvementIds.Contains(involvement.InvolvementAreaID) ? "checked" : "") />
                            <label class="form-check-label fw-semibold" for="interest_@involvement.InvolvementAreaID">
                                @involvement.AreaOfInvolvement
                            </label>
                            @if (!string.IsNullOrEmpty(involvement.Description))
                            {
                                <div class="text-muted small">@involvement.Description</div>
                            }
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>

    <button type="submit" class="btn btn-primary">
        <i class="bi bi-save"></i> Save My Selections
    </button>
</form>